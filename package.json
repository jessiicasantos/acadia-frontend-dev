{
  "name": "acadia-frontend-dev",
  "version": "1.0.0",
  "description": "Responsive Product Card",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch api/db.json && ws --spa --port $PORT --hostname 0.0.0.0 index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4",
    "local-web-server": "^5.4.0",
    "zod": "^4.3.6"
  }
}
