{
  "name": "acadia-frontend-dev",
  "version": "1.0.0",
  "description": "Responsive Product Card",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch api/db.json --static ./public --port $PORT --host 0.0.0.0",
    "dev": "json-server --watch api/db.json --static ./public",
    "test": "echo \"Error: no test specified\" && exit 1",
    "redeploy": "git commit --allow-empty -m \"Forcing Heroku deployment\" && git push heroku main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4",
    "zod": "^4.3.6"
  }
}
